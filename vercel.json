{
  "buildCommand": "pnpm -r --filter @workspace/urja-academy run build",
  "framework": "vite",
  "outputDirectory": "packages/urja-academy/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}